const nodemailer = require('nodemailer');

module.exports.newPost = function(post) {
    // send mail to the person who created the post -- using transporter.sendMail function
}

module.exports.deletePost = function(post) {
    // send the mail to the person who created the post

}